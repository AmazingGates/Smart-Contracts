[project]
name = "python-in-updraft-cu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lru-dict",
    "titanoboa",
]
